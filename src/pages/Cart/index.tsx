export interface IItem {
    description?: string,
    quantity?: string,
    imgURL?: string
}

interface ICart {
    items?: IItem[]
}

export default function Cart(){

    return (
        <>
        </>
    )
}